plugins {
    id 'java'
}

version 'unspecified'

sourceCompatibility = 1.8


dependencies {
    compile project(":owmclient.core")
}

idea {
    module {
        downloadJavadoc = true
        downloadSources = true
    }
}
